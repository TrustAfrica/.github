name: Node
on:
  workflow_dispatch:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
jobs:
  ci:
    name: Deployment
    uses: TrustAfrica/GHA.workflows/.github/workflows/gha.workflows.build-node.yml